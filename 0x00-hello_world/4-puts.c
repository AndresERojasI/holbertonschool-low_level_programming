#include <stdio.h>
#include <string.h>

/**
 * main - Entry point function
 *
 * Return: Always zero
 */
int main(void)
{
	char output[52];
	strcpy(output, "\"Programming is like building a multilingual puzzle\n");

	puts(output);

	return (0);
}
